import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import Name from './Name';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    /* <>
    <App/>
    <Name /> //this is a function which we written in app.js ...means make function in js and then render it .means use html from js.
    </> */
     <App/>
);


